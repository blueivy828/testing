name: CI Pipeline

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build:
    name: Build Next.js App
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Install pnpm
      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      # Step 3: Setup Node.js with pnpm caching
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      # Step 4: Install dependencies
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Step 5: Run lint and type-check
      - name: Lint & Type Check
        run: |
          pnpm run lint
          pnpm run type-check

      # Step 6: Build the application
      - name: Build application
        run: pnpm run build

      # Optional: Upload build artifacts
      # - name: Upload build artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: build-output
      #     path: .next/
      #     retention-days: 7

      # Job 2: Run Tests
    #   test:
    #     name: Run Tests
    #     runs-on: ubuntu-latest

    #     steps:
    #       - name: Checkout code
    #         uses: actions/checkout@v4

    #       - name: Setup Node.js
    #         uses: actions/setup-node@v4
    #         with:
    #           node-version: "20"
    #           cache: "npm"

    #       - name: Install dependencies
    #         run: npm ci

    #       - name: Run tests
    #         run: npm test
    # use: npm test --passWithNoTests
